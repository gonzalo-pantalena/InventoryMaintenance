# Copy to .env in this folder. Must match the password in appsettings.Docker.json
# and meet SQL Server sa password policy (8+ chars, upper, lower, number, symbol).
MSSQL_SA_PASSWORD=LocalDev!Mssql2026
